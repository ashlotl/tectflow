use winit::{event_loop::EventLoop, window::Window};

const STARTING_HEIGHT: u32 = 720;
const STARTING_WIDTH: u32 = 1024;

pub(super) struct TectflowRendererBoilerplate {
	//stuff, idk
	window: Window,
	event_loop: EventLoop<()>,
	// pub vulkan_context:
}
